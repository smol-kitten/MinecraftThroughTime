# AGENTS.md — MinecraftThroughTime

<!-- agentsmd:generated -->
> Auto-generated by `agentsmd.py` (deterministic, no AI) · 2026-07-30 · chore/agents-md-entry@9ec1d86.
> This is the canonical entry file for **any** coding agent. Edit only inside the custom block below — the rest is regenerated.

## What this is

A command-line tool that lets you create and use profiles to set a Minecraft client or server to a specific game version. Profiles contain date-based version schedules, allowing you to automatically progress through Minecraft's version history over time.

## Build / test / run

```bash
dotnet build MinecraftThroughTime.sln   # Build
dotnet test                             # Test
```

## Where to look (machine discovery)

These are kept fresh automatically — read them before grepping:

- `.claude/wiki/code-map.md` — who-calls-whom Mermaid graph
- `.claude/symbols.json` — machine symbol index (`symbols` skill / symbols.py)

## Contributing conventions

- Branch → PR → merge. Never commit directly to the default branch, never force-push, never `--admin`-merge.
- Keep this file's generated sections in sync by rerunning `agentsmd.py gen`; put durable hand-written notes in the custom block.

<!-- agentsmd:custom:start -->
<!-- Hand-written notes below are preserved across regeneration. -->

<!-- agentsmd:custom:end -->
